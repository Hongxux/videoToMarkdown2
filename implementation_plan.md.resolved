# Pipeline 并行化：串行 IO + 并行 LLM + VL 令牌桶限流

## 背景

当前 `task.queue.max-concurrent=1`，所有任务完全串行。用户希望：
- Download/Transcribe/Stage1 **保持串行**（I/O 密集、占 GPU/带宽）
- Phase2A/VL/Phase2B **跨任务并行**（LLM 调用为主）
- VL API 有速率限制 → 用**令牌桶**控制调用速率（而非限制线程）

## 资源特征

| 阶段 | 瓶颈 | 并发策略 |
|------|------|---------|
| Download | 网络带宽 | Semaphore(1) 串行 |
| Transcribe | GPU (Whisper) | Semaphore(1) 串行 |
| Stage1 | Python gRPC | Semaphore(1) 串行 |
| Phase2A | DeepSeek API (**无限制**) | 自由并行 |
| VL Analysis | VL API (**1200 RPM / 1M TPM**) | 令牌桶限流 |
| Phase2B | DeepSeek API (**无限制**) | 自由并行 |

## 设计方案

### 两阶段分离 + VL Token Bucket

```
┌─────────────────────────────────────────────────────────────────┐
│  IO Phase (Semaphore = 1)         │  LLM Phase (自由并行)       │
│  Download → Transcribe → Stage1   │  Phase2A → VL → Extract →  │
│  同时只允许 1 个任务执行           │  Phase2B                    │
│                                   │  VL 调用经过 TokenBucket    │
└─────────────────────────────────────────────────────────────────┘
```

**Timeline 示例（3 个任务 A、B、C）：**

```
Thread-1: [A: IO Phase] ──release──→ [B: IO Phase] ──release──→ [C: IO Phase]
Thread-2:                            [A: Phase2A → VL → Phase2B]
Thread-3:                                                        [B: Phase2A → VL → Phase2B]
                                                                 [A完成]
```

### VL Token Bucket（Python 端）

> [!IMPORTANT]
> Token Bucket 放在 **Python gRPC 服务端**，因为实际的 VL API 调用在 Python 层发起。

```python
# 令牌桶参数
VL_RATE_LIMIT_RPM = 1200        # 每分钟请求数
VL_RATE_LIMIT_TPM = 1_000_000   # 每分钟 token 数

# 双维度限流：
# 1. RPM 桶: 容量=1200, 每秒补充 20 个 token
# 2. TPM 桶: 容量=1M, 每秒补充 ~16,667 个 token
# 每次 VL 调用前 acquire(1) from RPM桶, acquire(estimated_tokens) from TPM桶
```

**实现位置**：新建 `services/python_grpc/src/content_pipeline/rate_limiter.py`

- 使用 `asyncio` 兼容的令牌桶实现
- RPM 桶：固定每次 consume 1
- TPM 桶：根据请求预估 token 数 consume（图片帧数 × 每帧 token + prompt token）
- 超限时 `await` 等待令牌补充，不拒绝请求

### 线程分配

| 配置项 | 值 | 说明 |
|--------|---|------|
| `task.queue.max-concurrent` | **3** | Worker 总线程数 |
| `task.pipeline.io-concurrency` | **1** | IO 阶段 Semaphore 许可 |

> [!NOTE]
> LLM 阶段不再需要单独的并发限制。DeepSeek 无速率限制，VL 由 Python 端令牌桶控制。
> 因此 `max-concurrent=3` 意味着最多 3 个任务同时执行，但 IO 阶段同时只跑 1 个。

## Proposed Changes

---

### Java 端：两阶段拆分

#### [MODIFY] [TaskProcessingWorker.java](file:///d:/videoToMarkdownTest2/services/java-orchestrator/src/main/java/com/mvp/module2/fusion/worker/TaskProcessingWorker.java)

1. 新增 `task.pipeline.io-concurrency`（默认 1）
2. 新增 `Semaphore ioSemaphore`
3. [processTask()](file:///d:/videoToMarkdownTest2/services/java-orchestrator/src/main/java/com/mvp/module2/fusion/worker/TaskProcessingWorker.java#159-217) 拆分调用：
   - acquire ioSemaphore → 执行 `orchestrator.processVideoIOPhase()` → release ioSemaphore
   - 执行 `orchestrator.processVideoLLMPhase()` (无额外限制)

#### [MODIFY] [VideoProcessingOrchestrator.java](file:///d:/videoToMarkdownTest2/services/java-orchestrator/src/main/java/com/mvp/module2/fusion/service/VideoProcessingOrchestrator.java)

1. 新增 `IOPhaseResult` 内部类（封装 videoPath、outputDir、duration、Stage1Result 等中间数据）
2. [processVideoInternal()](file:///d:/videoToMarkdownTest2/services/java-orchestrator/src/main/java/com/mvp/module2/fusion/service/VideoProcessingOrchestrator.java#267-562) 拆分为：
   - `processVideoIOPhase(taskId, videoUrl, outputDir)` → `IOPhaseResult`
   - `processVideoLLMPhase(taskId, ioResult)` → [ProcessingResult](file:///d:/videoToMarkdownTest2/services/java-orchestrator/src/main/java/com/mvp/module2/fusion/service/VideoProcessingOrchestrator.java#219-228)
3. 原 [processVideoInternal()](file:///d:/videoToMarkdownTest2/services/java-orchestrator/src/main/java/com/mvp/module2/fusion/service/VideoProcessingOrchestrator.java#267-562) 保留为包装方法

#### [MODIFY] [application.properties](file:///d:/videoToMarkdownTest2/services/java-orchestrator/src/main/resources/application.properties)

```diff
-task.queue.max-concurrent=1
+task.queue.max-concurrent=3
+task.pipeline.io-concurrency=1
```

---

### Python 端：VL 令牌桶

#### [NEW] [rate_limiter.py](file:///d:/videoToMarkdownTest2/services/python_grpc/src/content_pipeline/rate_limiter.py)

双维度令牌桶：
- `TokenBucket` 类：容量、补充速率、当前令牌数、`async acquire(n)` 方法
- `VLRateLimiter` 类：组合 RPM 桶 + TPM 桶，提供 `async acquire(estimated_tokens)` 接口
- 可配置参数通过环境变量 `VL_RATE_LIMIT_RPM` / `VL_RATE_LIMIT_TPM`

#### [MODIFY] VL 调用入口

在 VL API 调用前插入 `await vl_rate_limiter.acquire(estimated_tokens)`。具体修改位置需要定位到实际调用 VL model API 的 Python 函数。

## Verification Plan

1. **提交 3 个任务**，观察日志：IO 阶段串行，LLM 阶段并行
2. **VL 限流验证**：检查 Python 日志中令牌桶的 acquire/wait 记录
3. **取消测试**：多任务并行时取消其中一个
4. 运行现有测试：
```powershell
cd d:\videoToMarkdownTest2\services\java-orchestrator
mvn test -Dtest="TaskProcessingWorkerDownloadRetryTest,TaskProcessingWorkerUploadCleanupTest"
```
