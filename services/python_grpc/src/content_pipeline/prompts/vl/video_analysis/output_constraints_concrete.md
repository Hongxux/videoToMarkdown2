[硬性约束 - 具象化分析模式 (concrete)]
1) 格式: 必须且仅输出标准 JSON 数组。禁 Markdown 代码块 (```json)及前后缀释义。
2) 语言: JSON 文本字段必用中文。
3) 结构: 每一项代表一个核心视觉片段。
4) 字段定义:
   - 必填:
     - segment_id (Int): 序号，从 1 开始。
     - segment_description (Str): 短标题。
     - main_content (Str): 讲解与画面结合的富排版文本。用讲师第一人称。首行 `> **核心论点**：...`。用加粗标签+4空格缩进建逻辑树。涉视觉元素处句末精准插 `[KEYFRAME_{N}]`（N从1始）。**必须严格确保**该占位符总数量与截图数组长度分毫不差！若多个动作被合并为一张结果图，文本中只能有一一对应的一个占位符，严禁为无图的中间态盲目插入占位符导致数量超出。
     - clip_start_sec (Float): 开始时间。
     - clip_end_sec (Float): 结束时间。
     - instructional_keyframes (List): 画面时间戳对象。
       - keyframe_id (Int): 截图编号（N从1开始递增），必须与正文里的 `[KEYFRAME_{N}]` 完全相等且相互映射。
       - timestamp_sec (Float): 精确秒数。**全篇贯彻极简配图！绝对配额上限：每个大逻辑段最多1图**。拒碎片化流水账。**极度重要：对于任何能用纯文字讲清的配置或简单流程，强制留白不配图！** 绝对禁止截取：1.纯文本PPT大段文字/毫无界面的屏幕 2.初始空白 3.被遮挡/极度放大防迷失 4.翻页模糊变动 5.反选光标污染或初稿。
         时机要求(优先定稿)：1.定稿(代码/推演出最终全貌结果) 2.巨变(首次切换新画面稳定时) 3.聚焦(被指示/高亮/放大) 4.定格(图表播完展现全貌)。
       - frame_reason (Str): 描述如何引导用户看这张图，**此处的引导性词语必须与之对应节点的 `main_content` 讲解内容紧密配合，以辅助 `main_content` 讲解为核心目的。**语气必须模仿讲师口吻（例如：“大家请看图表右上方...”）。先指出需要看的信息的相对位置（借助元素、文字和在图片中的大体位置），说明需要看的信息的内容是什么，以及它如何作证/呼应正文的讲解内容。用于 Markdown 图注。
   - 可选 (若无则全省该键):
     - instructional_clips (List): 动态视频剪辑片段，当静态关键帧无法表达过程时使用。
       - clip_id (Int): 片段编号（N从1开始递增），与正文里 `[CLIP_{N}]` 占位符保持一致。
       - start_sec (Float): 开始相对秒数。
       - end_sec (Float): 结束相对秒数。**绝对最高红线：end_sec 减去 start_sec 必须 <= 5.0**！
       - clip_reason (Str): 描述如何引导用户看这段动画/视频，**此处的引导性词语必须与之对应节点的 `main_content` 讲解内容紧密配合，以辅助 `main_content` 讲解为核心目的。**语气必须模仿讲师口吻（例如：“大家请看画面中数据流的走向...”）。先指出需要关注的动态元素和过程发生的大体位置，说明这段动画展现了什么连贯性原理、过渡过程或逻辑流转关系，以及它如何作证/呼应正文的讲解内容。用于辅助 Markdown 视频注。
     - precautions (List[Str]): 易错点。
     - segment_summary (Str): 一句话总结。

5) 禁令: 严禁输出 reasoning/key_evidence/step_type 等无关内容。
6) 分段: 按逻辑/关键画面切换拆不重叠片。单片段可含多图。
7) 时间戳: 相对秒数，禁填-1。贯穿全片可填 [0.0, 视频总时长]。
    
8) 严格输出示例 (请深刻理解其中的无图与单图防泛滥逻辑):
```json
[
  {
    "segment_id": 1,
    "segment_description": "微服务概述(纯文本大纲回顾)",
    "main_content": "> **核心论点**：微服务架构拆分的本质是实现业务域的高内聚和低耦合。\n\n- **微服务核心优势**：在这个小节中，讲师主要用一页布满纯文字要点的 PPT 进行了理论铺垫。\n    - **单独部署**：每个服务独立进程运行，部署更灵活。\n    - **技术异构**：各服务可根据其负载特性自由选择最合适的语言和数据库组件。(注：这段仅是对纯文本 PPT 的宣读提取，没有任何架构图或界面操作支撑，根据红线特权本段强制零配图，坚决不瞎截 PPT 大字)",
    "clip_start_sec": 0.0,
    "clip_end_sec": 12.0,
    "instructional_keyframes": []
  },
  {
    "segment_id": 2,
    "segment_description": "分布式缓存架构改造详解",
    "main_content": "> **核心论点**：引入 Redis 缓存层是我们解决高并发瓶颈的关键战略。\n\n- **新旧架构版图对比**：接下来我们一起来拆解改造后的数据流转。\n    - **两段式拦截全貌**：来看这张画好的最终架构图。之前直连请求极易引发性能灾难，改造后系统会优先拦截查缓存，未命中再穿透查底层库。[KEYFRAME_1]\n    - **数据流转动态呈现**：当遭遇缓存击穿测试时，我们可以清晰看到测试流量由于未做单点防抖，瞬间蜂拥涌向底层 DB 的动态拥塞全记录。[CLIP_1]",
    "clip_start_sec": 12.0,
    "clip_end_sec": 35.0,
    "instructional_keyframes": [
      {
        "keyframe_id": 1,
        "timestamp_sec": 28.0,
        "frame_reason": "大家请看画面中部的新架构版图，新引入的 Redis 缓存层位于网关与 DB 之间并横向拦截了请求。由于严格限制单个大逻辑只留一幅图，因此我放弃截取讲师刚画的一半草图，而是合并等到整张复杂网络拓扑定稿时才截图"
      }
    ],
    "instructional_clips": [
      {
        "clip_id": 1,
        "start_sec": 29.0,
        "end_sec": 32.5,
        "clip_reason": "大家请看画面中部的架构层之间，这里必须要用动态视频，才能向大家清晰展示请求箭头像潮水一样从上层击穿中间件来到 DB 层动态流转全过程"
      }
    ]
  }
]
```
