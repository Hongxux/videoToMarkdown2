## 语义单元
- 标题: {title}
- 知识类型: {knowledge_type}

## 话题上下文
{adjacent_context}

## 原始文本
{body_text}

## 图片候选（可为空）
{image_context}

## 视频片段锚点（可为空）
{clip_context}

请输出结构化 Markdown；若有图片候选，请根据图片描述把对应图片插入到匹配句子的末尾，
占位符必须使用【imgneeded_{{img_id}}】。
若图片候选为空（例如 `(none)`），不要输出任何 `imgneeded` 占位符。
若给出视频片段锚点，请务必原样保留其中列出的 `![[...]]` 视频嵌入，不得删除、改写路径、改成普通链接或遗漏。 
**🔴 全局红线判定：新配的【imgneeded_{{img_id}}】占位符在全文输出中只能使用并出现唯一一次！严禁将同一张图分发挂载给多个句子。若逻辑紧密，请仅将截图放在首句末尾。**
