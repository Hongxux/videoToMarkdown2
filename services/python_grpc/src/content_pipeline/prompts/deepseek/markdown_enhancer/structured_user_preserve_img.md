## 语义单元
- 标题: {title}
- 知识类型: {knowledge_type}

## 话题上下文
{adjacent_context}

## 原始文本
{body_text}

## 图片候选（可为空）
{image_context}

## 视频片段锚点（可为空）
{clip_context}

请输出结构化 Markdown；若有图片候选，请根据图片描述把对应图片插入到匹配句子的末尾，
占位符必须使用【imgneeded_{{img_id}}】。
若图片候选为空（例如 `(none)`），不要输出任何 `imgneeded` 占位符。
**特别注意：请务必原样保留原文中的所有 Markdown 图片标识（例如 `![描述](链接)`），并保持其在文中的对应关系和相对逻辑位置不变！**
**特别注意：若原文或视频片段锚点中已经给出 `![[...]]` 视频嵌入或 `[CLIP_{N}]` 对应的视频媒体，请务必原样保留对应视频锚点，不得删除、改写路径、改成普通链接或遗漏。**
**🔴 全局红线判定：无论是原文自带的图片标识、还是新配的【imgneeded_{{img_id}}】，同一个图/链接在你的全文输出中只能出现一次！严禁重复使用同一张图配多个句子。若有重复，请仅保留首次出现。**
