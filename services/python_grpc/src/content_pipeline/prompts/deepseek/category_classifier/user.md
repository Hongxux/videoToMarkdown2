## 输入参数信息

- **视频标题**: 
{{video_title}}

- **第一段核心正文内容**:
{{first_unit_text}}

- **视频的所有大纲模块分组名称列表**:
{{group_names}}

- **现有分类路径库 (一行一个，格式：领域/子领域)**:
{{categories}}

---

请根据上述信息，深度理解视频的内容方向，从现有的分类路径库中挑选出最精准的分类。如果现有的分类无法精准覆盖视频内容的核心主题（或者现有分类不符合 MECE 规则），你**必须**基于我们设定的「MECE 两层结构分类铁则」和命名规范准则，**自行创造出一个全新的相互独立且唯一的两层级分类路径（领域/子领域）**。

输出包含 `category_path`、`is_new` 和 `reasoning` 短评的 JSON 结果。
