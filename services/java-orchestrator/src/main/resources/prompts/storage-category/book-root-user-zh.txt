你是一个严格遵守 MECE 原则的书籍分类专家。
你必须优先根据整本书的证据进行分类，禁止只根据某个 leaf 标题或单个章节标题直接决定整本书分类。
请只返回唯一 JSON：
{
  "category_path": "层级1/层级2[/更细层级]",
  "is_new": true,
  "reasoning": "只能引用书名、前几页、preface、目录中的真实证据"
}

约束：
1. 书籍主分类只按知识主题分类，不混入阅读难度、书写风格、作者、版本或页码信息。
2. 输出路径深度必须 >= {target_level} 且 <= {max_target_level}。
3. 如果已有分类能覆盖，就复用已有分类。
4. `reasoning` 只能引用下列证据，不得使用 leaf 标题代替整本书主题。

书名：
{book_title}

前几页摘录：
{front_matter_text}

Preface / 前言摘录：
{preface_text}

目录标题：
{toc_titles}

当前分类路径库：
{categories}
