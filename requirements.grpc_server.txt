# 运行 python_grpc_server.py 的推荐依赖集合
# 用法：
#   pip install -r requirements.grpc_server.txt
#
# 说明：
# - 本仓库历史上同时出现过 opencv-python 与 opencv-python-headless。
#   两者都会提供 `cv2`，但通常不建议同时安装；这里默认使用 headless 以减少 GUI 依赖。

# gRPC 基础
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.21.0

# 资源与并行
psutil>=5.9.0

# Stage1 pipeline
langgraph

# Video 下载/处理
yt-dlp
ffmpeg-python
aiohttp
aiofiles

# ASR（代码当前使用 faster_whisper）
faster-whisper

# CV
opencv-python-headless>=4.8.0
pillow>=10.0.0
pytesseract>=0.3.10

# 数据与工具
numpy>=1.24.0
scipy>=1.11.0
pandas
pyyaml>=6.0
rich

# LLM/消息队列（按需）
openai>=1.0.0
anthropic
sentence-transformers>=2.2.0
pika==1.3.2
webrtcvad
dtw-python

# 旧实现/文档用（当前主链路不依赖，但如你还在用 openai-whisper 可手动启用）
# openai-whisper>=1.0.0
